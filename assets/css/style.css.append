/* Icon-only actions on small screens */
@media (max-width: 640px) {
  .admin-view-btn span,
  .admin-delete span { display: none; }
  .admin-view-btn,
  .admin-delete {
    padding: 8px;
    width: 38px;
    height: 38px;
    border-radius: 10px;
    justify-content: center;
  }
}
